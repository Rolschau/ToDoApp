{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "LocalStorageConfiguration": {
    "EnableEncryption": false,
    "EncryptionSalt": "todosalt",
    "Filename": ".todo.localstorage",
     "__comment": "ToDo: consider making a separate Filename per user id eg. SSO like IdentityServer4 (OAuth/OpenID protocols, ASP.NET Core)."
  }
}
